This is the word-for-word transcript of the video:

00:00 - 01:18
"All right, so this video is going to cover the training process for the FLUX2 Klein 9B model. I've already trained a bunch of LoRAs for FLUX2 for the HerbstPhoto LoRA and kind of honed in on a good set of parameters. But we're going to expand those a little bit, we're going to test a bunch of different models, we're going to pick the best one and then we're going to keep iterating it until we have a model that is something that I'd like to publish.

All right, so FLUX2 Pro is an image generation model from Black Forest Labs, and it's probably the best one as far as quality, but the speed is—is really slow. And so I've already released the HerbstPhoto V4 model for FLUX2, and I want to do a training round for the smaller, newer model, which is FLUX2 Klein. And this is one that can run on smaller GPUs and inference a lot faster. In the training of the Pro model, I ran over 30 training runs to hone in on the best way to tag datasets and the best parameters. And so we're going to be starting from where I left off there and we're going to take kind of the hero winning set of parameters for the config file and then we're going to expand upon it and we're just going to keep training until we get something that is good enough to share."

01:19 - 02:39
"So, to give a little bit of an overview of this research—and this is a image generation set testing different parameters in the AI Toolkit config files, and each set of images is for a different training run testing a single parameter. And so there's about 40 different, 30 or 40 different versions of these as well as scheduler and sampler tests. And I'll go into this in more detail in a separate video because it's kind of its whole own beast, but really quickly, the first thing I did was test every single scheduler and sampler in the K-Sampler—every single combination. So there's like 300 of those.

And the one that I found was the best for FLUX2 for this—for this specific look—it's not for every look, you know, if you want to do animation or something that's more of a digital image that doesn't have as much grain or something more experimental, it might be a whole different set of schedulers and samplers. But after looking at all of them on a huge grid, I decided that dpmpp_2s_a + sgm_uni was the most filmic and the most grainy. And I did that test before testing any LoRAs—the base model on its own—so that way I know when I'm testing all of these models side-by-side, I'm using the most optimal scheduler and sampler for this—for this intention of image generation."

02:39 - 03:29
"So all of these images are generated with the dpmpp_2s_a ancestral and sgm_uni. And the first thing I did was test the network dimensions. And there's four different network dimensions parameters: there's the linear, the linear alpha, the conv, and the conv alpha. The winning combination was 128, 64, 64, and 32. Past that, it was diminishing returns, and when you cranked them all up to 256, the images got totally ripped apart, which is kind of beautiful and interesting but not what we want for this use case.

Then I tested the timestep type. And at the top here is always the default settings from Ostris where I just ripped the original training config. And so we can kind of see what happens when we change it to sigmoid and linear and shift. And shift gave some really interesting outputs, but again, this is one where the defaults were just better."

03:29 - 04:31
"In all of these tests, what you're looking at is—there's a photo of a man in a shop here, and this is—it's a prompt that's basically directly from the training data. And so this image reflects: can the model recreate the training data? And then there's a second image, which is just three words: 'dog on a log', forwards. And none of these tokens—dog or log or on a log or dog on a—none of those tokens are in the training data. And so this 'dog on a log' test is actually a better way to examine if the LoRA can transfer a style to a prompt that isn't in the dataset. Because if the dataset is just this batch of images... but what if I want to take this model—and that's the intention—and to use it on a new prompt and to make a new image? And so the best way to examine the LoRA's effect on an image is basically running it on a prompt that is completely outside of the training data and to see if the style of the training data is still learned in the neural network to the new prompt."

04:31 - 05:42
"I also tested the decay and found that lowering the decay by 50 to 100 to 200 percent had good effects for picking up on grain and still retaining a lot of detail. As you can see, there's a lot of texture in the shadows here, the highlights bloom nicely, there's a little halation, and this is all stuff that lowering the decay captured better than the defaults.

I also tested the other optimizer. The default is Adam and then the option here is Adafactor. For this case, I liked the default Adam. Then there's the timestep bias. We'll go into this more later, but essentially I just went with the defaults here as well, which is balanced.

The learning rate—this is something that's interesting because FLUX.1 and a lot of other models, the learning rate is really something you can tune up and down to help the model learn certain attributes or not learn certain attributes. But in this case, just changing it by—the default is 0.0001—just changing it by five one-thousandths of a percent totally ripped apart the image. And so for some reason with the FLUX2 architecture, you know, this set of tests shows you that okay, unlike other models, this one is unique. When you're training it, you probably shouldn't change the learning rate at all. And so moving forward, the learning rate we locked it for the rest of them."

05:42 - 08:31
"This is more network dimensions tests where we learned basically the same thing. The sweet spot is 128, 64, 64, 32, but I tested every other combination here. I'm not going to go into these images so much, I just want to breeze past all the parameters I've tested.

And then in this last round, I tested two different sets of network dimensions: this 128, 64, 64, 32 and a bigger one at the same ratio of 4 to 2 to 2 to 1 of 256, 128, 128, 64. And then I also tested a decay that was 10 times higher than the default, a decay that was 10 times lower than the default, and then the shift timestep bias and the default timestep bias, which is balanced.

And the HerbstPhoto FLUX2 V4 model uses these key parameter changes. It goes 128, 64, 64, 32 for the network settings, and then it uses a 10x lower decay from the default. And so now each of these images moving to the right is a different epoch of different steps of training. So all of these models here, all these main tests, these are all just done at 3,000 steps, which is a pretty good starting step count. But it's—it's better to run it for more epochs and higher steps once you find the right config. But it doesn't make sense to run all of these models at 10,000 steps because it just—it gets too expensive and too time-consuming. It doesn't make sense to test them all. You can kind of see at 3,000 steps if the model is learning what you want it to or not."

08:31 - 10:09
"And so each of these images moving to the right is a different epoch going from 1,000, 2,000, 3,000, 5,000, and then here's 7,000, which was my favorite. And then just to be sure to hone in on this, I also tested like just a little bit up—7250, 7500, 6750—and really landing at step 7,000 for this config file, for this base model—it's not universal—really gave me the—the subject retention with this highlight flare and the lens artifact particularly, and the tinted shadows and the grain and the halation, the bloom—like it really captures everything that's in the training data in the way that I wanted it to.

And so this is the one that I released for FLUX2. The V4 LoRA is number 46 of all my training runs. 128, 64, 64, 32, and then decay at 1 10,000th and 7,000 steps. And then just to be sure that this wasn't, you know, some random seed that was giving me a good run that I've been using for all this stuff, then I ran it against a bunch of different seeds with the same model, and it worked for all of them.

So that's the process of how I honed in to getting this final published model. So that's what we're going to start with now. Now we're going to do a similar process. We're not going to do all these tests because we kind of learned what we needed to, but we can hone in more, and we're going to do the similar process for the FLUX2 Klein model with the same dataset, with the same intention, and then we're going to side-by-side the two models and see how they compare with a fine-tuned LoRA."

10:09 - 11:47
"All right, so we're going to jump into RunPod. We're going to do a new GPU pod. The FLUX2 Klein 9B model, I think it's like 29 gigs, so we're going to need at least a card with 48 gigs of VRAM plus. I'm going to go with the Pro 6000. I'm actually going to grab six of them so that we can train six different models in parallel and work a little bit faster.

Going to edit the template, change template, and then Ostris AI Toolkit official template. We're going to edit this. I have no idea how many training runs I'm going to end up doing, so I'm just going to give myself way more storage than I need. Do 500 gigs here and maybe 250 here. I don't even know that that's necessary. We're going to set it and deploy.

While this gets booted up, we're going to go ahead and go to huggingface.com in the top left corner, go down and click access tokens. We're going to create a new token. It's going to be a read token. We're going to call it 'bla bla bla bla bla', create token, copy it. This is the only time you'll be able to see this token, so if you close this window, you'll have to make a new one."

11:47 - 14:18
"And then a few minutes later in our logs, we can see that it's ready. Now we go to connect, we click the port 8675. The password default is always 'password'. And now here is our six GPUs sitting idle. So the first thing we're going to do is we're going to go into our settings and we're going to paste that access token that we grabbed. We're going to press save. Once it says saved successfully, we're going to add our dataset.

So we're going to do new dataset. I have a lot of these datasets that I've created and tested. The best one that I have a copy of here is called 'HerbstPhoto', it's the version 2 of the dataset. We're going to create it, and then we're just going to drag its 41 image files with 41 caption files. The names of the captions have to be the same of the images. You can also caption them directly in AI Toolkit, but if you're going to be stopping the pod and coming back and doing trainings and want to use the same dataset, it's good to have this saved locally and then to just drop it right in.

All right, and I get this bug on AI Toolkit where whenever I'm running several GPUs on a single pod, the dataset stops at 100% and doesn't populate. I refresh it. So a quick fix, instead of accessing Jupyter and restarting the pod, is just going to be to drag in the image and caption files one at a time. So I'm going to do that here."

14:18 - 17:25
"All right, so now if we go to our datasets, we can see here it is. We're going to go to new job. And first we're going to grab the correct model architecture. It's going to be FLUX2 Klein base, the 9B version. I'm going to turn off low VRAM because we have plenty of VRAM. And we're going to call this 'FLUX2 Klein 9B HerbstPhoto'. We're going to call this version zero. It's going to be the Ostris default settings, so that way when we test the other models that we train on with a different parameter configuration, we can A-B to test to see how the parameter changes affect the outputs.

The trigger word is already in all of the training data, but I'm going to add it here just to be safe. I don't know that this is necessary. And the only things I'm really going to change... the sweet spot for the FLUX2 Pro model that we found was that 7,000 step one. I'm going to overshoot that a little bit. I'm going to train for 10,000 steps. And I'm just going to save it every 1,000 steps and we're going to save 10 epochs. So that means that at the end of this training run, there's going to be 10 different versions of the model with training steps 1,000 through 10,000.

We're going to make sure we have our dataset selected. We're going to go ahead and just train on all resolutions. And we're going to go ahead and turn off sampling, disable sampling. We're going to kill all these captions here. And you can enable sampling and monitor the progress of how the LoRA is training, but it will have to take the time to do all those generations. And even though this is a fast model, I'm going to go ahead and test every single one of these epochs inside of Comfy where I have a little more control. So I'm just going to skip this step, and that should do it.

So, we're going to go ahead and click show advanced, and this is the config file. There's a whole bunch of other hidden settings in here. We're going to mess with some of those in the next version. But we're going to create this job and we're going to run it. Now, before I go ahead and make all the other versions of it, I'm just going to monitor this. Oh, and here we go. I need to first go to the model page here and agree and access repository. That's a step that I skipped. So now let's try running this again."

17:25 - 20:01
"All right, so in the meantime, let's make the other configs and just queue them up and hopefully there's no problems with the first one that I'm cloning here. And so let's go ahead and clone this job, and then we're going to assign it to the next GPU, GPU 1, and we're going to call this version one. And the only thing I'm going to change here is the network dimensions, like we talked about before. It's these four lines of code, 14 through 17. The linear, we're going to do 128. The linear alpha, we're going to do half of that, 64. The conv is going to be the same as the linear alpha, that's going to be 64. And the conv alpha is going to be half of the conv, that's going to be 32.

And so you can see it only shows the linear rank here, 128, but if you go into the advanced view, this is where we can actually make these numbers a staggered value, and that's going to have a good effect on the model being able to learn fine-grained details. And then I'm just going to rename this with these parameters so that way when I'm testing it, I can know what I'm testing. So we're going to call this 128, 64, 64, 32. Everything else is exactly the same, there's no sampling, 10,000 steps, saving every 1,000 steps with the same dataset. We're going to create this job and we're going to queue this one.

So now if you look at the queue here, and we're just going to rinse and repeat this process but the different GPUs get assigned the different training jobs. Now we're going to clone this one, we're going to assign it to GPU number 2. We're going to call this LoRA version two. And I'm quite sure that the weighted timestep is going to be our best bet based on the FLUX2 test I did. But I'm going to go ahead and just do a test with sigmoid, otherwise everything else is the same. We're going to create the job, we're going to run it. Go back to the training queue and then we're just going to repeat, clone the job, GPU 3. I also want to test the shift because that has interesting results in my tests with FLUX2 Pro. Create the job, version three on GPU 3. The same here, we're going to clone. All right, now we're going to clone this one, and this is going to be GPU number 3, version number 3. We're going to put this back to weighted."

20:01 - 22:18
"And in these FLUX2 Pro tests, like I highlighted before, I thought the most differentiating parameter in this dataset training is the decay. The default decay is 1/1,000th and so in the most successful LoRA that I had for the Pro model, I just divided it by 10 and trained on 1/10,000th for decay. So this job right here is essentially going to be a clone of the version 4 model that I released. So I would bet that this one is going to be the most successful, but I'm also going to go ahead and train two more versions with the decay variable. So I'll just also label this decay 0.0001, and we will create this job.

Now two more. We're going to clone it. This is going to be version 4. And for this one, I'm going to see what happens if we push the decay even lower. So now we're going to do—we're going to add a zero and it's going to be 5/100,000ths for the decay. I'm going to designate that here in the name as well. Everything else is the same, we're going to create it, queue it. And then lastly, we're going to clone it. This is going to be to our last GPU, we're going to call it version number 5. And for this one, we're going to effectively multiply the successful decay from the Pro run and we're going to do a higher version of it. Still not the same as the default value; this default is three zeros and a one, the most successful version I had before was 10x that at four zeros and a one. We're going to go and split the difference, and so now we're going to do three zeros and a five. What a horrible way to describe those numbers. Anyhow, everything else should be good. We're going to create it, we're going to play it."

22:18 - 25:34
"And so let's just check the status of this first one. Each iteration is taking 1.47 seconds. How the hell long is this going to take? How long to train 10k steps? So it's going to be about five hours of training, but I'm going to stop the video and come back once it's gotten to about step 3,000 for all of them. And then I'm going to download those models and we're going to start testing to see if any of these are significantly better or worse than the other, and then we're going to see how it stacks up to the FLUX2 Pro model.

All right, so it's been about an hour and I have each of the six LoRAs trained up to 3,000 steps. And so I've got this workflow here in Comfy that's going to generate seven images. The first one's going to have no LoRA, the second is going to have the Ostris default, and then so forth the—the rest are going to be the other LoRAs that we were training that are still running in the background now. Again, this is just the 3,000 step version to kind of check the status of how they're doing.

The latent size here is going to be 1024 by 1024. Now all the training data is 1920 by 1080, and so this is a great way to check if the LoRA can continue to apply its learned look across different dimensions. So again, just kind of how like we're testing this simple prompt that isn't in the training data in any of these tokens, we're also going to be testing a resolution that isn't in the training data. And the seed and the steps and the flux guidance, this is uniform for all of these. It's all using this dpmpp_2s_ancestral sgm_uni combo that I was talking about before. And in my FLUX2 test, I kind of found the sweet spot for getting the most out of this LoRA before the image rips apart is 0.73. This might change for this base model, I'm not sure how it's going to perform with these LoRAs, so I'm actually going to go ahead and wire up a new float value for all of the model weights. This one's not getting any model, I'll turn this one off here.

And this is just so that if I change the LoRA strength here, it will change uniformly on all of them and I don't forget to accidentally leave one of them out. So I'm just wiring this up. I know it looks like a mess, I'm just doing this for speed and not trying to make it pretty. And I'm using the LoRA Stack JK node because what's really great about it is you can pipe out the LoRA metadata, which sends a string of the LoRA name, and then I pipe that into a label that goes onto the image. So that way when I'm saving these images and looking at them in a grid, I can identify which LoRA is attached to which image and not have to, you know, open files and dig around and try to remember which LoRA is which config.

And then that's the beauty of naming them with the config parameters that we did in AI Toolkit is because now these images will display with the not only the name of the LoRA but embedded in that is the—the parameters that were the variables. So I'm going to go ahead and run this, it'll probably take an extra couple minutes because it has to load the model weights, but otherwise this should be pretty quick. And so we're going to run this now."

25:34 - 28:55
"In the meantime, I'm going to go ahead and double back and we're going to start downloading these later training runs. They've already made a lot of progress. I'm just going to grab the most recent one and we can always walk back if we want the other epoch. We're going to grab the 6,000 step for all of them because they've all completed the 6,000th step.

And this is just a little hack, but sometimes when uploading files to RunPod, the HTTP upload speeds either are really slow even with good bandwidth locally or the files don't upload completely. And so the best way to do it is to get everything uploaded on Google Drive and then I just go in ChatGPT and tell GPT the links, the names of the files and then the destination path, and it will just give you a download command that uses GDown and fuzzy and then it will use the RunPod download speed, which is super fast. So the only bottleneck here is just downloading the files from RunPod and uploading them to Google Drive.

Now obviously the fastest way to do this would just be using AI Toolkit on your Comfy machine, and then whenever the files are saved on the machine, you can just immediately migrate them with move commands into the Comfy folder. But I'm using the RunPod template just because it's easier and quicker, and so I'm just eating that download and upload speed. But I have good internet, so it's no problem. But if you really wanted to optimize this, you could just get AI Toolkit installed on the same volume that Comfy is running and then as soon as the models are downloaded there's no need to be stopped by any downloading or uploading."

28:55 - 33:02
"All right, so let's have a look. So here's the base model on its own, here's with the Ostris defaults, and then here's the Ostris defaults with a bigger network dimension. Here's with sigmoid, here's with a 10x lower decay, here's a 20x lower decay, and then here's to increase decay.

And so I would say that what I found here is basically exactly what I found in the FLUX2 training, and so maybe it wasn't necessary to fire up six GPUs. But for all intents and purposes, it's good to see how each base model performs differently. I would say that, you know, this dog is a little distorted. We'll see the different epochs, we'll grab this one, the version 3. But you can already see just out of the gate the default LoRA settings give a nice texture to the image.

Increasing the network dimension—what we see here is the grain pattern here is—it's learned it but it's—it's a little bit too aggressive and it's distorted the dog quite a bit. The dog here is still distorted, but the grain pattern is much more natural, and that's because with these larger network dimensions, the model was basically able to look into more fine-grained areas and pick up details. In almost every case, it's better to train with higher network dimensions, topping out at like 128. In the instance that maybe there are times where it's good to train with lower network dimensions, like if we are training a model to make a logo in a really small area, it's not needing to learn the details of an entire image and, you know, the three-dimensional spaces and a huge dynamic range of colors and all these texture patterns. Like it might just be a really simple vector, you know, color on color or color on fabric or color on background, and that's much simpler for the neural network to learn obviously, and so the network dimensions don't need to be so large. The notebook can be smaller, it's an easier subject to learn.

But generally most style LoRAs and most character LoRAs and most of the reason that we do training is because we're trying to teach the model fine-grained things that we can't do with prompts, and so again, it's usually beneficial... I've never found a use case with a style LoRA where it doesn't benefit us to have lower network settings. I don't know why that ratio of 4 to 2 to 2 to 1 works the best, but I've tested every combination of network dimensions from 32 to 256 with the ratios 1 to 2 to 4 in every category, so that's like I think I've done like 64 runs of all the different network combinations and—and this one was always outperforming the other one, so I'm pretty sound that across models that's like a uniform parameter that can be—can be relied on."

33:02 - 37:07
"All right, and let's just run this against some more seeds and see how we're doing. So these are really the ones we're looking at. It is interesting too that increasing the decay—this is a decay of 0.0005, which is 5x the default decay—it makes it have this really punchy contrast, like super cooked look, which isn't what I'm going for. But again, it's not necessarily wrong because this is just a really interesting case study of we have all this training data that has a really specific look that we're targeting, but we can tune the config file and these parameters to make a model that produces images that isn't necessarily like the training data, but it uses the training data and uses these parameters to make a look that's really interesting. This is obviously a lot more intense, it looks like—it looks really like old 110 super small film that's expired. Which is interesting because there's none of that in the dataset, but it's funny how changing a single parameter in a config file can make a model that outputs a—a real film look of a real—of a real stock that wasn't even inside of the training data.

So it's kind of funny, I see these—these complicated config files, you know, this mumbo jumbo... this is kind of to me like a meta language for camera settings. And so even though this is a lot of like computer math, I try not to focus on what the actual mathematical processes that are happening inside of all of these things. I come from like a really production lens camera photo background, and so I just try to focus on, you know, what happens when I turn this knob up or down. Does it make it more contrasty? Does it make it more blooming? Does it make it more cartoony? And then I don't really read or look so much into the technicality of what this stuff is actually doing, to me it's all really obsolete what these things are, they're just random knobs for me to turn and I get to figure out, you know, what knobs turned give what effect. The fun thing here is there's so many more of these than camera settings, and this is just like the Ostris Toolkit version. Like inside of each of these lines is a whole batch of processes happening, and you could get super nerdy and really dive deep into this stuff, but this is kind of the threshold of how technical I like to get. I don't want to dig so much further under here because I'm much more focused on the outputs than on the—the processes which get there."

37:07 - 40:13
"Yeah, these—again, and this is just a great use case on the network dimension increasing from the defaults to the defaults just with these bigger network dimensions. The fidelity gets so much better, the details get so much better, maybe not in this one, you know, there's a bit of randomness in the seeds. But overall the pattern that we'll observe is that the network dimensions increasing is always a good thing, and then case-by-case based on the look that we're going for for the training data, things like the decay and the timestep bias have a—have a strong effect.

So now that I know that this V3 one is kind of our favorite, I'm going to go ahead and—oh, this is burning my wallet. I'm going to go ahead and stop these other ones. I'm still paying for the GPU, so I'm going to fire up some other training. Stop this one. And we're just going to keep letting V3 go further. We'll also go ahead and download—we'll download all of these four, two, one, and I'll go ahead and clone this job.

All right, so now we're going to go ahead and call this one... what did we leave off at? Version 5. So this is going to be version 6. And I'm going to train this model something that's called float32. And this is kind of something that's buried in the config file that in my previous tests have always made the models better. It takes much longer to train, so it's more expensive, so this parameter is only one that I change when I'm like okay, this is a good model, I'm not messing with all the settings, now I'm just figuring out what else can I add on top of it to really polish it off.

The save type should always be the default for the model that it comes with. So like this is—we're going to leave this at BF16. But if we go into the advanced and we command-F D-type, we're going to notice that there's actually two of these D-types set to BF16, even though only one of them is in this simple view. And the one in the simple view is the save. So we're going to save it at BF16, but the one that we want to change is this BF16 one. So we're just going to go and type float32 here. And then I'm just going to indicate that and type FP32. This training might fail, I don't remember if I'm supposed to type float32 here or FP32. But we're going to create this job. Actually I'm going to make sure it goes to... let's go to GPU 0, so we're not training on the same GPU. And everything else is the same, and now we're run this one."

40:13 - 41:02
"The other thing that I'm going to do is—and I've never done this before and I've never seen it done, so this is—I'm kind of pushing past training models and now I'm getting into testing some theoretical concepts that I've been curious about, which is training a high-noise version of the model and a low-noise model. And this is something that Wan 2.2 does. If you're familiar with Wan 2.2, it's a video-based model, you can also use it to make images, but there's a high-noise version and a low-noise version. And there's two sampling steps, so in like the first K-Sampler, the high-noise model pipes in and in the second K-Sampler, the low-noise model pipes in.

And the noise, high noise vs low noise, is how the AI networks build images. They start with high noise and they go to low noise. And so the high noise layers are the early steps. So if you're sampling 20 steps, like steps 1 through 10 is considered high noise and steps 10 through 20-ish is considered low noise. And those first 0 through 10 steps, even those first 0 through 3 steps, if you're watching the denoise process, it's basically like looking at a blurry image and then you see the pure blacks and the pure whites and you see maybe where the sky is and where the people is. And it's a super blurry image of just the—the darkest and the lightest areas of the image. And this is essentially reflecting to us what our composition is going to be, because the texture and the lighting and the face details and the art direction is all stuff that gets baked in into the—the low noise, the stuff that happens later. And so that high-noise sampling that starts in the beginning really gives you your composition and that low-noise sampling that happens at the end really gives you your image texture."

41:02 - 41:31
"Now most models, like all the FLUX models, out of the box, you know, there's one base model. It's not a high-low base model like the Wan 2.2. And so what I'm going to test is by training this high-noise model, we can pipe that with the FLUX base model into a first K-Sampler and do the early steps with a model that's trained specifically on the high noise, the composition. And then it will pipe into a second K-Sampler with the same base model with a new low-noise trained LoRA that will learn more detail, the texture of the images. Because basically right now when we train a model with the balanced timesteps—which is, I can show you this... here we go, it's balanced—so when we train with balanced timestep, it's treating composition with the same weight as texture basically. And so this model is learning the composition and the texture."

41:31 - 41:49
"We are going to now do a high and low noise. So I'm going to keep everything the same here and I'm just going to change this to high noise. We're going to call this 3.1 because it's just a iteration of three and we'll call it high noise. Then we'll send this to GPU number 1. And then we're going to do the same thing and we're going to make the low noise. I don't know if any of that makes sense, but I'm hoping that when I show it in a Comfy workflow, maybe it will be a little more clear."

41:49 - 42:33
"But the benefit I'm thinking, hypothetically, is that right now this model learns everything evenly. And so maybe it might miss some of the composition trends in the model because it's also looking at texture and vice versa. So if we have one of the same model, same dataset, same parameters, but it's looking more at the high-noise composition, and then we only use that in the early sampling steps, and then we do the same in reverse with the low-noise model and it's only learning the texture and we only use that model in the low-noise steps. It'll give an image that is more refined, more reflective of the training data, and ultimately you have more control because the other thing you can decide is at which step do we switch models. Are we doing 20 steps and we switch at three or are we doing 20 steps and we switch at 15? So again, this is just me yambling, mambling, thinking out loud. So if you've made it this far through the video, bless your soul."

42:33 - 42:47
"I'm going to clone this job, call it 3.2, we'll assign it to GPU 2, and we will call this the low-noise version. This concept of low-noise and high-noise is super important in making images with a lot of control over the aesthetic. And it's almost like one of those gravel sifters where first the holes in the grate are really big and so only the big stuff falls through, and then they're a little smaller and so the smaller stuff falls through, and it's like sifting. And that's kind of what this denoise process is—like the first steps of denoising, it doesn't go and look at every single pixel really finely, it just grabs those big chunks and it will in the very early steps of the image you'll be able to tell if there's a person in the foreground, are they on the left or the right. It will be rendered as just a big blob but you'll be able to tell, and so that's the high-noise part of the sampling."

42:47 - 43:31
"All right, so this image here was created with the FLUX2 Dev model, which is the bigger, more flagship model. And again with the same dataset, the same decay, the same network dimensions, the same everything. And then this one on the left is just trained with the smaller Klein 9B base model. And so what we can see here is that, you know, the winner between the two models obviously as expected is still, you know, the Dev model. And this is with the same seed and the same prompt. And we can kind of see how with a different base model, the—the image is fundamentally different. It's not just rendering it with a lower filter, it's going to—it's going to render that noise pattern completely differently and give you a similar image, but it's not going to be the same image."

43:31 - 44:49
"It's interesting too that this lower Klein model to me, you know, the grain pattern here is—is much stronger with these same settings. It almost looks like this is 16mm film and this is 35mm film. I think the light leaks are actually more interesting, they're more textural, like there's a higher level of degradation that's probably because the images themselves with the base model only have a higher level of degradation and that gets amplified even with the LoRA.

And again, here's the base model Klein without the LoRA compared to our best one with the LoRA. And this really shows that, you know, out of the box if you just put a simple prompt in these models, like they don't look great. Even the Dev model doesn't look great on its own. And so you might say oh, I'm trying all the AI models with the same prompt and the same settings and this one is better than this one. But the—the real way to weigh how good these models are is how trainable they are. Because the benefit of an open-source model like FLUX or an open-weights model where we can train it is just that and we can train it. And so when we're evaluating these models, we really are underselling them if we look at what the models do out of the gate without any training. Because these models are generic by intention so that way they can be fine-tuned by the user. Whereas if you have a closed-source model like Nano-Banana or Midjourney or something like that, like maybe the images look more polished out of it, but you can't—you can't do all this training and tuning to determine the exact decay level's relationship to the grain and—and the light leak patterns. Like yes, maybe this model out of the box looks worse than Midjourney, but ultimately if you are training and tuning it, it's so much more powerful of a tool because the aesthetics you can get are anything that you can collect a dataset of."

44:49 - 46:11
"And before anyone says it, I know I can make all of these into concat grids, but I like to just keep them in these windows so I can move them around and kind of compare. I know how to use those nodes, I'm choosing not to. So this is an interesting case study of the four images on what's happening when we change the decay. This one here, the third most, is the default decay, which is 0.0001. This is 5x higher than that, 0.0005. This is 10x lower, 0.00001. And this is 20x lower, 0.000005.

And I have no idea what's happening behind the curtain, the mathematical function of what the decay is and how it relates to the training data and the neural network. But I don't really care, again, I'm just seeing this as an obsolete knob to turn, and I'm trying to understand what happens when we turn that knob. And do we want to turn it up for this use case or do we want to turn it down for this use case? And there's usually use cases where you want to turn it up and there's other ones where you want to turn it down. All this stuff is completely subjective, it comes down to taste, it comes down to looking at an image and saying this is what I want and this is what I don't want. But again, this stuff is measurable. And by doing all these AB tests where we change one parameter at a time and then looking at them in a graph with a waveform is a really great way to think rudimentarily about what are these parameters doing to the image and when I'm training my dataset, what is the intention for my output and then how do I configure these parameters to get the desired output."

46:11 - 46:45
"We're going to dig into this decay level even more because I think it's such an important parameter and it really shows how we can examine the technicalities of these parameters and how they affect the actual—the pixels of the image. So what we're looking at here is those four levels of decay. This is the 20 times lower than the default settings, this is the 10 times lower than the default settings, which is my favorite for this use case. This is the default settings and then this is the 5x higher than the default settings.

And below that is an RGB waveform where each pixel in this image corresponds and it analyzes the red and the green and the blue channel of each pixel and the luminance of them and it plots them on this grid. So if we even just turn this image off and only examine the waveforms, the pattern here is really—is really distinct and it's really interesting. That out of the gate, if you look at the, for instance, this red channel, it's—it's a little bit narrow and then as we lower the decay, it gets wider and as we make the decay even lower, it gets they—they all get so wide that they blend together. And so what that blending is that we're seeing the waveforms that's corresponding directly to the decay in the config file is, you know, you might say oh, the contrast is decreasing or oh, the colors are getting more muted. But it's a real measurable attribute. And so even though what we're looking at is oh, how does decay affect style, what we're talking about when we're talking about style is—is something that is actually measurable. And there—there is a more esoteric part of style, it's just about taste and what you like might be a waveform that looks like this on one and a waveform that looks like this on the other. But it is—it is something that is measurable."

46:45 - 47:31
"The other thing that's measurable here is the—the lowermost point, that's the black point. So in this image, the far-left one, the green channel hits the lowest point, the blue channel almost hits the lowest point, the red channel is still low but it doesn't hit the lowest point. And so what we're seeing is—is this, and that's what that looks like is when your red channel doesn't hit black but your blues and your greens basically do. And you have something that is close to pure black. And then on the other end of the spectrum, every single channel is significantly above the lowest black point. And that basically makes an image that has a—a higher black point.

The other interesting thing is that what's not being affected is the white point. On the inverse, in both these images, all the channels basically touch the brightest most luminance value to some degree. And so what we can take away from this is if we are training a dataset that is supposed to look like camera log or a really flat washed out photography look, then this 20x lower decay is actually a parameter that we want to—to run with. Whereas if we want to train a really cooked graphic novel pure black and pure white manga style, this really high decay is actually probably the parameter we want to run with."

47:31 - 48:30
"All right, so while these training jobs continue to run, I still have two of these fucking expensive GPUs sitting idle, so I'm just going to—we're going to train something with them for God's sake. This is the bad part about renting a cluster, I don't know if there's a way to stop this individual GPU, whereas if you just rent a pod with one GPU and then do that again and again and again and again, then when you're done training on that pod you can stop the pod and stop spending money on it. But this we kind of bottleneck ourselves into a situation where we have to be training on all of them simultaneously in parallel so that way none of them are sitting idle. It's easier because you can just upload the dataset once and use the config and clone the job across the GPUs, but I don't usually do it like this, I'm just doing it as fast as I can for the purpose of not having to edit this video for so long. So we're just going to clone this again and what other settings can we change?"

48:30 - 49:56
"So this will be version 7. So we'll call this V7 and I don't really know what effect on the image this LOKR will do and this LOKR factor, I have no idea, so we'll just turn this up to we'll say 16, nah, 32, I don't even know what that means. So we'll go LOKR at 32 is this version. And this is one of those parameters, there's so many parameters in this config file, this is something I haven't really tested. So well we have this GPU, let's go ahead and do that one. Now this should go to the GPU number 4, update it, and then we'll call this version 8. This is a clone of V3, the best one, and another thing I'm going to test is the caption dropout rate. I think this has something to do with how strict the prompts align to the training data. So let's just go ahead and try cranking this up to 0.6, which is pretty high, and then we'll indicate that in the title here, caption drop 0.6, create the job and make sure we're on the correct GPU and then we'll run it.

Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too, I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient accumulation. We'll do this one parameter. All right, and really quickly let's just guesstimate how long it will take to run one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days."

49:56 - 50:52
"I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out. It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."

50:52 - 51:57
"All right, so it's the next evening, all the models have reached at least training step 3,000, so we're just going to take a quick re-examination of them. It's going right to left, this is the base model only, version 0, 1, 2 and so forth. And then in the bottom corner we have the—the one of the best ones, the version 3, 7k steps. The new ones are the FP32 model, which thankfully in this case for this FLUX model, it seems like the FP32 trainings are worse and they cook the images too much. I don't think that's a global parameter trueness for all base models, for instance with Wan I found that the FP32 training was a lot more beneficial for learning details. Version 7, this is with EMA, it's worse. Version 8, this is with differential object preservation, and I'm pretty sure this will make the model function as an editing model. I'm kind of just changing one parameter at a time here continuing this pattern of AB testing.

The batch size and the gradient accumulation will make the render time higher. So again, I'm just going to save those for last. Let's do caption dropout, we'll 10x this to 0.5. That's probably kind of crazy, let's go 0.25. Actually I had really good luck with Wan training caption dropout as 0.12. So let's just say point point... I'm so fucking indecisive right now. It doesn't matter, caption drop 0.2 for version 9 without differential output preservation, and then queue it."

51:57 - 53:05
"Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too. I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient... this one... here's one parameter.

All right, and really quickly let's just guesstimate how long it will take to run on one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days. I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out.

It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."

53:05 - 54:02
"All right, so it's the next evening, all the models have reached at least training step 3,000, so we're just going to take a quick re-examination of them. It's going right to left, this is the base model only, version 0, 1, 2 and so forth. And then in the bottom corner we have the—the one of the best ones, the version 3, 7k steps. The new ones are the FP32 model, which thankfully in this case for this FLUX model, it seems like the FP32 trainings are worse and they cook the images too much. I don't think that's a global parameter trueness for all base models, for instance with Wan I found that the FP32 training was a lot more beneficial for learning details.

Version 7, this is with EMA, it's worse. Version 8, this is with differential object preservation, and I'm pretty sure this will make the model function as an editing model. I'm kind of just changing one parameter at a time here continuing this pattern of AB testing. The batch size and the gradient accumulation will make the render time higher. So again, I'm just going to save those for last. Let's do caption dropout, we'll 10x this to 0.5. That's probably kind of crazy, let's go 0.25. Actually I had really good luck with Wan training caption dropout as 0.12. So let's just say point point... I'm so fucking indecisive right now. It doesn't matter, caption drop 0.2 for version 9 without differential output preservation, and then queue it."

54:02 - 55:18
"Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too. I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient... this one... here's one parameter. All right, and really quickly let's just guesstimate how long it will take to run on one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days. I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out.

It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."

55:18 - 56:12
"All right, so it's the next evening, all the models have reached at least training step 3,000, so we're just going to take a quick re-examination of them. It's going right to left, this is the base model only, version 0, 1, 2 and so forth. And then in the bottom corner we have the—the one of the best ones, the version 3, 7k steps.

The new ones are the FP32 model, which thankfully in this case for this FLUX model, it seems like the FP32 trainings are worse and they cook the images too much. I don't think that's a global parameter trueness for all base models, for instance with Wan I found that the FP32 training was a lot more beneficial for learning details. Version 7, this is with EMA, it's worse. Version 8, this is with differential object preservation, and I'm pretty sure this will make the model function as an editing model. I'm kind of just changing one parameter at a time here continuing this pattern of AB testing.

The batch size and the gradient accumulation will make the render time higher. So again, I'm just going to save those for last. Let's do caption dropout, we'll 10x this to 0.5. That's probably kind of crazy, let's go 0.25. Actually I had really good luck with Wan training caption dropout as 0.12. So let's just say point point... I'm so fucking indecisive right now. It doesn't matter, caption drop 0.2 for version 9 without differential output preservation, and then queue it."

56:12 - 57:12
"Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too. I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient... this one... here's one parameter. All right, and really quickly let's just guesstimate how long it will take to run on one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days. I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out.

It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."

57:12 - 58:12
"All right, so it's the next evening, all the models have reached at least training step 3,000, so we're just going to take a quick re-examination of them. It's going right to left, this is the base model only, version 0, 1, 2 and so forth. And then in the bottom corner we have the—the one of the best ones, the version 3, 7k steps.

The new ones are the FP32 model, which thankfully in this case for this FLUX model, it seems like the FP32 trainings are worse and they cook the images too much. I don't think that's a global parameter trueness for all base models, for instance with Wan I found that the FP32 training was a lot more beneficial for learning details. Version 7, this is with EMA, it's worse. Version 8, this is with differential object preservation, and I'm pretty sure this will make the model function as an editing model.

I'm kind of just changing one parameter at a time here continuing this pattern of AB testing. The batch size and the gradient accumulation will make the render time higher. So again, I'm just going to save those for last. Let's do caption dropout, we'll 10x this to 0.5. That's probably kind of crazy, let's go 0.25. Actually I had really good luck with Wan training caption dropout as 0.12. So let's just say point point... I'm so fucking indecisive right now. It doesn't matter, caption drop 0.2 for version 9 without differential output preservation, and then queue it."

58:12 - 59:12
"Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too. I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient... this one... here's one parameter. All right, and really quickly let's just guesstimate how long it will take to run on one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days. I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out.

It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."

59:12 - 60:12
"All right, so it's the next evening, all the models have reached at least training step 3,000, so we're just going to take a quick re-examination of them. It's going right to left, this is the base model only, version 0, 1, 2 and so forth. And then in the bottom corner we have the—the one of the best ones, the version 3, 7k steps.

The new ones are the FP32 model, which thankfully in this case for this FLUX model, it seems like the FP32 trainings are worse and they cook the images too much. I don't think that's a global parameter trueness for all base models, for instance with Wan I found that the FP32 training was a lot more beneficial for learning details. Version 7, this is with EMA, it's worse. Version 8, this is with differential object preservation, and I'm pretty sure this will make the model function as an editing model. I'm kind of just changing one parameter at a time here continuing this pattern of AB testing.

The batch size and the gradient accumulation will make the render time higher. So again, I'm just going to save those for last. Let's do caption dropout, we'll 10x this to 0.5. That's probably kind of crazy, let's go 0.25. Actually I had really good luck with Wan training caption dropout as 0.12. So let's just say point point... I'm so fucking indecisive right now. It doesn't matter, caption drop 0.2 for version 9 without differential output preservation, and then queue it."

60:12 - 60:41
"Training all these at 7,000 steps is really overkill, but I'm just like if I'm going to spend 10 hours training the FP32 model and I have to wait 10 hours, I'm just going to let these ones run too. I think they'll each take probably like three hours. So again, this is probably still too much. Okay, last one I swear, last one I swear, I'm so hungry. This one will be gradient... this one... here's one parameter. All right, and really quickly let's just guesstimate how long it will take to run on one, two, three, four, one, two, three, four, five, six—42,000 steps of training. 37 hours of training. All right, maybe I'll come back in two days. I will probably just once the FP32 model is done, which is on step 81, I'll just stop these wherever they are. And then we'll just test whatever we have and—and the best one is what we'll push out.

It's interesting that on this L40S it's training at the same speed of an H200. I don't—if someone could explain to me how to make—like is there a purpose in using faster graphics card to train models or is it just like have I been using H200s way too much and I could have just been using L40s the whole time? That's ridiculous. Good night."
